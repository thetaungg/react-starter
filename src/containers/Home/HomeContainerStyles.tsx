import { css } from '@emotion/react';

export const container = css``;

export const robotsContainer = css`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    row-gap: 20rem;
    column-gap: 20rem;
    padding: 2rem 5rem;
    margin-top: 10rem;
`;
